{
  "name": "designaanvraag",
  "private": true,
  "version": "0.1.0",
  "description": "NBC designaanvraag-tool: Typeform-achtig formulier \u2192 Asana-taak voor de designer, met automatische huisstijl-extractie.",
  "workspaces": [
    "shared",
    "web",
    "worker"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "test": "npm run test -w web && npm run test:worker",
    "e2e": "npm run e2e -w web",
    "lint": "npm run lint -w web",
    "sync-shared": "node scripts/sync-shared.mjs",
    "test:worker": "node --test --experimental-strip-types --no-warnings worker/test/*.test.mjs",
    "test:functions": "cd supabase/functions && deno test --allow-env --allow-net=none _shared/ submit-aanvraag/ aanvraag-status/ asana-webhook/"
  },
  "engines": {
    "node": ">=22"
  }
}
